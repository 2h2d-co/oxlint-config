import type { DummyRuleMap } from "oxlint";

/** Optional review signals that must not block lint or require suppressions. */
export const advisoryRules: DummyRuleMap = {
  "2h2d/no-unknown-returns": "warn",
};
